{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Terminal de débogage de JavaScript",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}